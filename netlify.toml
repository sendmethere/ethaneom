[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# React Router를 위한 SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
